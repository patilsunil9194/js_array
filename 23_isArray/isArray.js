// isArray - used to check if a given value is an array

const arr = [1, 2, 3, 4, 5];
const str = "12345666";
const num = 34;

console.log(Array.isArray(arr)); // true
console.log(Array.isArray(str)); // false
console.log(Array.isArray(num)); // false
