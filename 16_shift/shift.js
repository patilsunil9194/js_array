// shift - used to remove the first element from an array.
// this method will also return the removed element

const numbers = [1, 2, 3, 4, 5];

let removed = numbers.shift();

console.log(removed); // 1
console.log(numbers); // [2, 3, 4, 5]
